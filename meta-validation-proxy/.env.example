# Server port (default 3002)
PORT=3002

# The Creative Router sends the Meta token in the Authorization header on each
# request; this proxy does not store the token. No META_MCP_API_KEY needed here.
